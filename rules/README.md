# Agents rules

Reference rules for Claude Code, Codex, Cursor, and other agents.

The [skills CLI](https://skills.sh/) does **not** install this folder. Copy a rule into your agent's rules directory if you want it active.
