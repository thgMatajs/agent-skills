# Coding

Skills for writing code in any language or stack.

| Skill | What it does |
| --- | --- |
| [`reuse-before-write`](./reuse-before-write) | Before new production code, search the current repo for a callable API and return `reuse` or `write-new` after a fit-first audit. |

Add another skill here as `skills/coding/<skill-name>/SKILL.md`.
